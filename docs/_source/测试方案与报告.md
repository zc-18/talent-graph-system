# 测试方案与测试报告

> 智岗图谱（TalentGraph AI）　|　题目 XH-202621
> 对应赛题"可验证性要求"：提供完整测试方案（含≥100 条岗位 JD 及测试用例），核心指标可量化验证。

---

## 一、测试目标

| 指标 | 赛题要求 | 本系统实测 | 结论 |
|------|----------|-----------|------|
| JD 解析准确率 | ≥90% | **98.25%（F1，P 98.59% / R 97.98%，n=371）** | ✅ 达标 |
| 简历提取准确率 | ≥90% | **96.49%（F1）/ 召回 100%（n=18）** | ✅ 达标 |
| 人岗匹配准确率 | ≥90% | **100%（分类准确率 / match F1 均 100%，n=15）** | ✅ 达标 |
| 单元测试覆盖率 | ≥60% | **65%（85 个用例全部通过）** | ✅ 达标 |
| 测试 JD 规模 | ≥100 条 | **对抗基准 379 条 + 生产真实语料 2306 条** | ✅ 达标 |

---

## 二、测试数据集（双轨制）

系统采用**双轨数据集**：

- **轨道①　对抗测试夹具 / 合成解析基准**（`docs/测试数据/seed_jds.json`，379 条）：人工可控注入抄袭、能力通胀等对抗样本，配套逐条真值标签，用于**可复现的定量评测**（清洗检出率、JD 解析准确率）。该数据集**不进入生产图谱**，仅作评测基准。
- **轨道②　生产真实语料**（**2306 条真实 JD，5 类渠道 / 6 个平台批次**）：全部为合法合规采集或公开许可的真实数据，构成 2018 / 2024 / 2026 三个时间切片：

  | 平台批次 | 条数 | tier | 权威度 | 时间切片 | 说明 |
  |---|---|---|---|---|---|
  | `feishu_ats` 飞书招聘 SaaS 企业官网 | 629 | official | 1.0 | 2026-07 | 11 家企业官方招聘官网（蔚来/小鹏/得物/EcoFlow/零一万物/生数科技/出门问问/Insta360/猎户星空/合众新能源/爱奇艺）；采集 1829 条，领域过滤后入库 629 条 |
  | `iguopin` 国聘网 | 571 | gov | 1.0 | 2026-07 | 中央广播电视总台主办的国家级公共招聘平台；采集 633 条，领域过滤后入库 571 条 |
  | `dataset_51job2018` | 300 | dataset | 0.7 | 2018-12 | 历史基线，GitHub 公开仓库 north-jewel/data_analysis |
  | `dataset_aijob2024` | 300 | dataset | 0.7 | 2024-07 | 中间基线，HuggingFace `RocXuLi/AI_Job_DataSet_1000_list`（**AFL-3.0 许可证**） |
  | `tencent` 腾讯招聘 | 255 | official | 1.0 | 2026-07 | 官网公开接口，批次 2026W31-r1 |
  | `netease` 网易招聘 | 251 | official | 1.0 | 2026-07 | 官网公开接口，批次 2026W31-r1 |

  SimHash 查重在该语料上检出 **150 条重复**（去重后有效 2156 条），能力通胀检测标记 **409 条** JD。线上系统（DB `talent_graph_v3`）的岗位/技能/演化图谱**全部由该真实语料构建**：岗位 29 个（新兴 6 + 既有 23；人工智能 15 / 大数据 6 / 智能系统 5 / 云计算与工程 2 / 物联网 1）、技能 4561 项（粗粒度 567 + 细粒度技能点 3994）、岗位-能力关系 2823 条（另 1403 条低置信被过滤）、证据 5409 条 + 权威佐证 8 条。

- **合规说明**：本轮对 **39 个候选数据源**逐一做了 robots.txt 与入口实测，**全部聚合类招聘平台（BOSS直聘 / 智联 / 猎聘 / 拉勾 / 牛客 / 前程无忧 / 实习僧）一条数据都未采**——原因分别为 robots 明确禁止职位检索路径、WAF 人机校验、JS 反爬令牌、自定义字体加密等；需登录或学生认证才能下载的数据集（阿里天池、和鲸 Heywhale、textdata.cn）同样未下载。逐条实测证据见 `backend/data/collect/数据源实测报告.md` 与 `采集合规说明.md`。

### 2.1 对抗基准 JD 数据集（`docs/测试数据/seed_jds.json`）

- **规模**：379 条 JD，覆盖 **31 个**新一代信息技术岗位簇，横跨 **人工智能 / 大数据 / 智能系统 / 物联网 / 云计算与工程** 五大技术栈：
  Java 开发、机器学习、大数据开发、算法、自然语言处理、计算机视觉、大模型应用开发、推荐/搜索算法、知识图谱、自动驾驶系统、服务机器人、数字孪生、嵌入式软件、边缘计算、工业物联网、云原生、MLOps 等。
- **真实性设计**：每条 JD 在职责措辞、技能子集、动词表述、排版模板上充分变化（多模板 + 同义动词），贴近真实"相似但不相同"的招聘文本。
- **对抗样本（用于验证数据清洗）**：
  - **抄袭样本 8 条**：整段复制已有 JD 仅改公司名/来源（验证近似去重；系统另自动检出 3 条模板高度相似的近重复，共 11 条）；
  - **能力通胀样本 17 条**：在正常技能外注入 6 个与岗位明显无关的冷门技术词（区块链、量子计算、FPGA、同态加密等，验证通胀检测）。
- **真值标签**（`ground_truth.json`）：每条 JD 标注真实的必备/加分技能、是否抄袭、是否通胀，用于自动化评测。

### 2.2 简历测试集（`docs/测试数据/test_resumes.json`）

- **规模**：18 份简历，覆盖 10 类目标岗位，匹配度分布从高到低（含部分匹配、较大差距样本）。
- **鲁棒性设计**：刻意使用同义词/缩写（torch、k8s、NLP、ML、数仓…）考验"抽取 + 归一化"能力。
- **真值标签**：每份简历标注真实技能集合。

---

## 三、测试方法与用例

### 3.1 JD 解析准确率

- **方法**：对 371 条非重复 JD 调用大模型解析，抽取必备/加分技能并归一化，与真值标签比对。
- **指标**：精确率 P、召回率 R、F1、重要度分类正确率。
- **结果**（`data/eval_jd_result.json`，n=371）：

```
精确率 Precision : 0.9859
召回率 Recall    : 0.9798
F1               : 0.9825
重要度分类正确率  : 0.9775
```

### 3.2 简历提取准确率

- **方法**：对 18 份简历解析抽取技能，与真值比对（软技能不计精确率惩罚）。
- **结果**（`eval_resume_result.json`）：

```
精确率 Precision : 0.937
召回率 Recall    : 1.000
F1               : 0.965
```

### 3.3 人岗匹配准确率

- **方法**：对每份简历与其目标岗位图谱，逐"必备技能"判定"具备/缺失"，与真值（候选人真实技能 ∩ 岗位必备技能）比对。
- **结果**（`eval_match_result.json`）：

```
分类准确率 : 1.000
匹配 F1    : 1.000
```

### 3.4 数据清洗有效性

| 项目 | 植入 | 检出 | 准确 |
|------|------|------|------|
| 抄袭/重复（对抗基准） | 8 | 8（+3 自动检出近重复） | 100% |
| 能力通胀（对抗基准） | 17 | 17 | 100% |
| 真实语料查重（2306 条生产语料） | — | SimHash 检出 150 条重复（去重后 2156 条） | 与人工抽检一致 |
| 真实语料通胀标记（2306 条生产语料） | — | 409 条 JD 标记 `inflation_flag` | 通胀技能因缺乏交叉验证被自动过滤 |

### 3.5 单元测试与覆盖率

- **框架**：pytest + pytest-cov，**85 个用例全部通过**，覆盖清洗、归一化、反幻觉聚合、置信度公式、匹配、演化、分级画像、JD/简历/新岗位定义抽取后处理、API 端点等。
- **覆盖率：65%（≥60% 达标）**。核心服务层（清洗、反幻觉聚合、置信度、同义词归一化、匹配）均在 85% 以上；未覆盖部分集中在外部调用封装（LLM / 联网检索，依赖网络）与一次性编排脚本，这部分由集成/冒烟测试与线上验证覆盖。

### 3.6 既有岗位能力动态演化验证（核心功能②）

- **方法**：把三个**独立来源的真实语料切片**按时间顺序依次灌入同一条演化服务链——2018-12（`dataset_51job2018`）→ 2024-07（`dataset_aijob2024`）→ 2026-07（腾讯 / 网易 / 飞书 ATS / 国聘），每一步都做真实 diff，自动标注「新增 / 删除 / 修改」并溯源（data_source 记录真实平台名、JD 数与批次）。
- **规模**：**共 621 条真实演化记录**——新增 367 / 淘汰 143 / 修改 111；按批次 `2024hist-r1` 128 条、`2026W31` 493 条；版本分布 **v1 14 个岗位 / v2 9 个 / v3 6 个**。
- **三点完整链条**：15 个传统岗位在 2018 语料中即存在；其中 **6 个 AI 族岗位**（机器学习、自然语言处理、计算机视觉、深度学习、推荐算法、嵌入式软件）在 2024 切片中同样有样本，因而拥有 2024 中间态、到 2026 演化到 **v3**。
- **"新岗位涌现"的直接证据**：另外 **14 个岗位**（大模型算法、AI 智能体开发、具身智能、数字孪生、大模型推理优化、AIGC、多模态、边缘计算、自动驾驶、机器人算法、数字人、提示词工程、AI 产品、生成式 AI 测试员）在 2018 语料中**完全检索不到**，只能单独建 v1 —— 不是我们宣称它们新，而是历史语料里根本不存在这些岗位。
- **在线可见**：`/evolution` 页面历史记录 + 岗位详情「演化历史」标签（新旧对照）。
- **示例**（Java 开发工程师 2018 → 2026 节选）：

| 变更 | 能力项 | 更新说明 | 数据源 |
|------|--------|----------|--------|
| 🟢 新增 | 大语言模型 | Java 后端开始要求对接大模型能力中台 / AI 网关 | 2026 多平台真实 JD 交叉验证 + 联网佐证 |
| 🟢 新增 | 检索增强生成（RAG）| 知识库问答 / 智能客服场景驱动后端集成 | 2026 多平台真实 JD 交叉验证 + 联网佐证 |
| 🟢 新增 | 向量数据库 | 伴随 RAG 落地成为后端新增数据组件 | 2026 多平台真实 JD 交叉验证 |
| 🟢 新增 | 云原生 | 云原生架构（Service Mesh/Serverless）持续渗透后端 | 2026 多平台真实 JD 交叉验证 |
| 🟡 修改 | Kubernetes | 容器化部署由「加分」升级为「必备」 | 2026 多平台真实 JD 交叉验证 |
| 🟡 修改 | 微服务 | 需求热度上升（权重显著提升）| 2026 多平台真实 JD 交叉验证 |
| 🔴 淘汰 | Struts | 在最新窗口 JD 中一次未再出现，判定为需求消退 | 窗口 JD 数 + 批次（`data_source` 落库） |
| 🔴 淘汰 | J2EE / Hibernate / SpringMVC | 同上，经典 SSH/SSM 技术栈集体退场 | 同上 |
| 🔴 淘汰 | Memcached / SQL Server / Dubbo | 同上 | 同上 |

### 3.6.1 淘汰（delete）判据与一次真实缺陷修复

**上一轮的结果是"删除 0 条"，我们没有接受"数据没给淘汰信号"这个解释，而是回去读了 diff 实现，结果发现删除分支在原理上永远不可能触发。**

- **缺陷**：调用方在做新一轮交叉验证聚合时，会把旧能力作为 `history` 先验来源注入（本身是对的，为了让历史证据参与打分）。于是旧能力名必然出现在聚合结果 `new_caps` 里，判据 `name not in new_caps` **恒为假**——删除分支是死代码。
- **更严重的连带问题**：若某旧能力在新窗口支持不足被降级为候选，它**三个分支都进不去**（不是新增、判不出删除、也不满足重要度/权重显著变化阈值），于是**变更日志里一条记录都不留、静默消失**——一个本该被审计的能力衰减事件从链上蒸发。

**修复后的四条判据**（`app/services/evolution.py::compute_changes`，先定判据再看结果，非事后调参）：

| # | 判据 | 定这条的实测依据 |
|---|---|---|
| ① | 传入 `window_skill_names`（**先验注入之前**、最新窗口 JD 里真实出现过的技能名集合）；淘汰判据 = "该能力在最新 N 条 JD 中**一次都没出现**" | 用聚合结果判淘汰在原理上判不出来；改用窗口原文后每条淘汰都可回溯到具体 JD 举证 |
| ② | **只对「必备能力」的消失**下淘汰结论 | 加分项进出频繁、不同语料用词差异极大。实测计入加分项会一次刷出 **160+ 条假"淘汰"**（Java 岗 189 条里 166 条是加分项），真信号被淹没 |
| ③ | **窗口 JD 数 ≥20** 才允许下淘汰结论（`MIN_JDS_FOR_DELETE = 20`） | 门槛设 10 时实测出现假阳性：只有 **16 条 JD 的运维岗把 Nginx 判成了"淘汰"**。提到 20 后该假阳性消失，样本薄的窗口（运维 16 条、大数据平台 13 条）**主动不下结论**——缺证据不等于反证 |
| ④ | 仍出现在新 JD 里但支持减弱、被降为候选的能力记为 **modify**（"降级为候选能力项，保留观察，未淘汰"） | 补上静默丢弃的漏洞，保证任何能力状态变化都在变更日志里留痕 |

**修复后实测结果**：三切片共产出 **143 条淘汰 + 111 条修改**（此前为 0 / 52）。淘汰分布：Java 开发工程师 23 项、后端开发工程师 14 项、数据分析师 9 项、数据仓库工程师 6 项、云计算工程师 1 项、物联网开发工程师 1 项等。

- **Java 开发工程师 23 项**：Struts、J2EE、Hibernate、SpringMVC / Spring MVC、Tomcat、Memcached、SQL Server、Dubbo、Spring Cloud、Oracle、Apache、MyBatis、多线程、Shell、KVM、HTTP、网络编程、分布式系统设计、CSS、HTML、Django、Flask —— 经典 Java 老技术栈集体退场；
- **数据分析师 9 项**：SAS、numpy、scipy、决策树、逻辑回归、聚类算法、聚类分析、神经网络、时间序列算法 —— 传统统计 / 机器学习工具链让位。

> **必须如实说明的一条**：Java 岗 23 项里的 **Django、Flask 属于 2018 语料的聚类污染**——这两个 Python Web 框架在建 2018 基线时被误归入 Java 岗位簇，它们的"淘汰"反映的是历史聚类噪声被新语料修正，**不是真实的技术退场**。我们把它写出来而不是悄悄剔掉：判据是机械执行的，没有为了结果好看做人工挑选；这条噪声也正好指向下一步要改进的岗位聚类精度。

> **判据取舍的诚实交代**：③ 明确**牺牲了一部分召回**——样本薄的岗位一条淘汰都不出。我们认为"漏判"比"错判"代价小得多：一条 Nginx 被判淘汰的假阳性，足以让整套演化结论的可信度归零。

> 验证「既有岗位能力动态更新」闭环：变更项可量化、可溯源、可人工优化，岗位定义随市场数据持续进化。

### 3.7 置信度校准（说明）

- **统一公式**：C = 0.35·支持率 + 0.20·来源多样性(平台数/3 封顶) + 0.15·时效性(半衰期 180 天) + 0.20·来源权威度(官网/政府 1.0、公开数据集 0.7、网络 0.6) + 0.10·外部验证；五个因子逐项落库（`job_skill.factors`），前端点击置信度徽章可见分解。
- **校准过程**：初版来源多样性上限为 5 平台，在当时仅 3 个采集平台的语料上系统性压低得分；据此将 DIVERSITY_CAP 由 5 调整为 3，并将岗位整体置信度改为**粗粒度 active 能力项的权重加权平均**。该校准做过一次后即**冻结参数**，不做逐岗调参。
- **结果**：全库岗位置信度均值 **0.6403**（上一版真实语料下为 0.535）。
- **提升来源必须说清楚——不是调参**：本轮**公式、五因子权重、多样性封顶值（3）与过滤阈值（0.45）全部原封未动**。均值上升的原因是语料本身：数据源从 3 个渠道扩到 5 类渠道 / 6 个平台批次后，同一技能最多可被 **5 个独立来源**交叉验证（来源池：2018 数据集 / 2024 数据集 / 腾讯 / 网易 / 飞书 ATS / 国聘），"来源多样性"因子更容易达到封顶；同时新增的飞书 ATS（tier=official）与国聘（tier=gov）权威度均为 1.0，把"来源权威度"因子均值一并抬高。即 **证据变厚了，不是分数被调高了**，这正是交叉验证机制"数据越多、判断越可信"的预期表现。

### 3.8 分级画像评估

- **规模**：**29 个岗位全部**建有 初/中/高 三档分级画像（`job_level_skill` 共 **1627 条**记录），构档规则：**每档 ≥3 条 JD 且岗位 ≥2 档**才产出，避免小样本档位噪声。上一版仅 22 个岗位达标，本轮语料扩容后实现全覆盖。
- **可验证性**：中级→高级晋升 diff 可在线查询（如 AI智能体开发工程师：晋升到高级需新增掌握 云原生 等 8 条能力项）。

### 3.9 已知局限（如实记录）

- **三个时间切片的来源丰富度不对等**：2018 与 2024 两个切片**各自只有单一公开数据集来源**，交叉验证机制在这两个切片上天然拿不到多源支持，因而这两个切片确认下来的能力项数量偏少；2026 切片则有腾讯 / 网易 / 飞书 ATS / 国聘四个独立来源。
- **由此带来的解读警告**：演化变更绝大多数落在 2026 这一步（**493 / 621 条**），**其中一部分来自来源丰富度的提升，而非纯粹的岗位能力增长**。这一点在解读演化曲线时必须打折看待，我们不把它包装成"八年间能力爆发式增长"。
- **淘汰结论同样受语料用词偏好影响**：淘汰判据虽以"最新窗口 JD 原文中一次未出现"为准、可逐条举证，但窗口语料的用词偏好仍会渗入结果——Java 岗 23 项淘汰中的 **Django / Flask 是 2018 语料的聚类污染**被新语料修正，不是真实技术退场（见 3.6.1）。此外，样本薄的窗口按判据③**主动不下淘汰结论**，因此 143 条淘汰是"下限"而非全量。
- **改进方向**：补齐历史年份的多源语料（更多年份的公开数据集、政府平台历史归档），使各切片的来源结构可比，即可把"来源丰富度效应"从真实能力演化中剥离出来。
- **2024 数据集的字段局限**：`dataset_aijob2024` 只有「岗位名 + 技能标签 + JD 正文」，无 company / location / salary 字段（落盘时留空而非臆造），且样本集中在 AI/算法方向、不含 Java/后端/数据分析等传统 IT 岗，因此这些岗位簇仍只有 2018 与 2026 两个观测点。

---

## 四、复现方式

```bash
# 1) 对抗基准数据集（评测轨道）
uv run python data/generate_dataset.py
uv run python data/generate_resumes.py
uv run python data/run_pipeline.py --reset --use-cache   # 基于合成基准评测清洗/解析

# 2) 生产真实语料（生产轨道，目标库 talent_graph_v3）
uv run python data/migrate_202609.py --db talent_graph_v3
uv run python -X utf8 data/collect/run_collect.py --platforms tencent,netease --batch 2026W31-r1
uv run python -X utf8 data/collect/run_collect.py --platforms feishu_ats,iguopin --batch 2026W31-r2
uv run python -X utf8 data/import_raw.py --batch 2026W31-r1 --tier official
uv run python -X utf8 data/import_raw.py --batch 2026W31-r2 --tier official --filter-it   # 飞书 ATS（企业官网）
uv run python -X utf8 data/import_raw.py --batch 2026W31-r2 --tier gov --filter-it        # 国聘（国家级平台）
uv run python data/run_pipeline.py --from-db             # 真实 JD 构建图谱
uv run python -X utf8 data/run_evolution_batch.py --batch-label 2024hist-r1   # 2018→2024 切片
uv run python -X utf8 data/run_evolution_batch.py --batch-label 2026W31       # 2024/2018→2026 切片
uv run python -X utf8 data/seed_new_jobs.py              # 6 个证据背书的新兴岗位
uv run python -X utf8 data/build_levels.py               # 初/中/高分级画像

# 3) 准确率评测（三项核心指标）
uv run python data/evaluate.py all

# 4) 单元测试 + 覆盖率
uv run pytest --cov=app --cov-report=term-missing
```

---

## 五、结论

系统三项核心准确率指标（JD 解析 F1 98.25% / 简历提取 F1 96.49% / 人岗匹配 100%）**全部显著超过 90% 的达标线**；数据清洗对抄袭、通胀对抗样本实现 100% 检出，并在 **2306 条真实生产语料**上稳定运行（SimHash 检出 150 条重复、通胀标记 409 条）；单元测试 85 个全部通过、覆盖率 65% 达标。测试方案完整、指标可量化复现，能力图谱基于 5 类渠道 / 6 个平台批次的真实招聘数据构建、三个时间切片的演化链条真实可查，精准贴合岗位实际需求；同时对"两个历史切片单一来源"这一方法论局限如实记录、不做粉饰。
