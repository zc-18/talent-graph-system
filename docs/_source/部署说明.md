# 部署说明

> 智岗图谱（TalentGraph AI）　|　题目 XH-202621
> 在线系统：http://101.200.184.201:8200/

系统采用**后端同端口托管前端静态资源**的一体化方案：一个 FastAPI（uvicorn 单 worker）进程即同时提供 REST API 与前端页面，无需额外 nginx，部署轻量、对小内存服务器友好（实测常驻内存约 162MB）。

---

## 一、环境要求

- Python ≥ 3.11（生产服务器使用 3.12）
- Node ≥ 18（仅前端构建需要，建议本地构建）
- MySQL ≥ 8.0（utf8mb4）
- 可访问：DeepSeek API、BGE 向量服务、Tavily、Serper.dev

---

## 二、方式一：systemd + venv（生产推荐，轻量）

### 1. 本地构建前端

```bash
cd frontend
npm install
npm run build        # 产出 frontend/dist
```

### 2. 打包并上传

```bash
# 将后端 app/、.env、requirements.txt 与 frontend/dist（作为 static）打包上传到服务器 /opt/talent-graph/backend/
#   /opt/talent-graph/backend/app
#   /opt/talent-graph/backend/.env
#   /opt/talent-graph/backend/requirements.txt
#   /opt/talent-graph/backend/static      (= frontend/dist)
```

### 3. 创建虚拟环境并安装依赖（仅预编译 wheel，不在服务器编译）

```bash
cd /opt/talent-graph/backend
python3 -m venv .venv
. .venv/bin/activate
pip install --no-cache-dir --only-binary=:all: -r requirements.txt
```

### 4. 配置并启动 systemd 服务

```bash
cp deploy/talent-graph.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable --now talent-graph
systemctl status talent-graph
curl http://127.0.0.1:8200/api/health
```

`talent-graph.service` 关键配置（含内存保护）：
```ini
ExecStart=/opt/talent-graph/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8200 --workers 1
MemoryMax=420M      # 内存上限，超出自动重启，保护小内存服务器
CPUQuota=120%
Restart=on-failure
```

### 5. 初始化数据（首次部署，建议本地执行后写入云 MySQL）

真实数据生产链路（目标库 `talent_graph_v3`，本地 PowerShell 先 `$env:DB_NAME='talent_graph_v3'`）。当前生产图谱由 **2570 条真实 JD、5 类渠道 / 6 个平台、15 个平台采集批次**构建：

```bash
cd backend
uv run python data/migrate_202609.py --db talent_graph_v3                    # 幂等 schema 迁移（新表 + 新列）

# 1) 合法采集（robots+控频+PII 护栏）→ data/raw/{batch}/
uv run python -X utf8 data/collect/run_collect.py --platforms tencent,netease --batch 2026W31-r1 --per-query 12
uv run python -X utf8 data/collect/run_collect.py --platforms feishu_ats,iguopin --batch 2026W31-r2

# 2) 批次 jsonl 入库（URL 去重、级别推断、新一代信息技术领域过滤）
uv run python -X utf8 data/import_raw.py --batch 2026W31-r1 --tier official               # 腾讯 255 + 网易 251
uv run python -X utf8 data/import_raw.py --batch 2026W31-r2 --tier official --filter-it   # 飞书 ATS 629（11 家企业官网）
uv run python -X utf8 data/import_raw.py --batch 2026W31-r2 --tier gov --filter-it        # 国聘 571（国家级平台）
#    历史切片：2018hist-r1（51job2018 公开数据集 300）、2024hist-r1（HuggingFace AI_Job 数据集 300）

uv run python data/run_pipeline.py --from-db                                 # 用真实 RawJD 构建图谱（生产路径）

# 3) 三时间切片真实演化（2018 → 2024 → 2026，按时间顺序跑，共产出 621 条变更记录：新增 367 / 淘汰 143 / 修改 111）
uv run python -X utf8 data/run_evolution_batch.py --batch-label 2024hist-r1
uv run python -X utf8 data/run_evolution_batch.py --batch-label 2026W31

uv run python -X utf8 data/seed_new_jobs.py                                  # 6 个证据背书的新兴岗位 + 权威佐证
uv run python -X utf8 data/build_levels.py                                   # 初/中/高分级画像（32/32 岗全覆盖，1076 条）
uv run python data/calibrate_confidence.py                                   # 置信度校准（一次性校准后冻结，本轮未再调整）
```

（对抗测试夹具路径，仅用于评测基准，不进生产库：`generate_dataset.py` → `run_pipeline.py --reset --use-cache`。）

---

## 三、方式二：Docker（容器化部署）

提供多阶段 `deploy/Dockerfile`（第一阶段构建前端，第二阶段运行后端并托管静态资源）与 `deploy/docker-compose.yml`（限制 420MB 内存 / 1 CPU）。

```bash
cd talent-graph-system
docker compose -f deploy/docker-compose.yml up -d --build
# 访问 http://<server-ip>:8200/
```

> 注意：在内存 < 2GB 的服务器上构建镜像可能紧张，建议本地构建镜像后 `docker save`/`docker load` 到服务器，或直接采用方式一。

---

## 四、配置项（backend/.env）

```ini
DB_HOST=...        DB_PORT=...     DB_USER=...   DB_PASSWORD=...   DB_NAME=talent_graph_v3
DEEPSEEK_API_KEY=...    DEEPSEEK_BASE_URL=https://api.deepseek.com   DEEPSEEK_MODEL=deepseek-chat
TAVILY_API_KEY=...      SERPER_API_KEY=...
EMBED_API_KEY=...       EMBED_BASE_URL=http://.../v1   EMBED_MODEL=bge-small-zh-v1.5   EMBED_DIM=512
APP_PORT=8200
```

> **数据库切换与回滚**：当前生产库为 `talent_graph_v3`（2570 条真实招聘语料构建的图谱）。切库 = 修改服务器 `.env` 的 `DB_NAME` 后 `systemctl restart talent-graph`；**回滚** = 将 `DB_NAME` 改回 `talent_graph_v2`（上一版 806 条语料图谱）或 `talent_graph`（最初的演示库），两者数据均保留未动，重启后秒级生效、无需改代码。

---

## 五、运维

```bash
systemctl restart talent-graph      # 重启
journalctl -u talent-graph -n 100   # 查看日志
systemctl show talent-graph -p MemoryCurrent   # 查看内存占用
```

**更新发布**：本地重新 `npm run build` → 替换服务器 `static/` 与 `app/` → `systemctl restart talent-graph`。
